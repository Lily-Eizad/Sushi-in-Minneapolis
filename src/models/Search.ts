//since we only have a single search param, I actually don't need
//to create a new type, but I like to demonstrate the use of such models
//since they are more practical for real-world applications
export type SearchParams = {
  location: string;
};
